console.log(`at the start variables.js`);

let x = 10;

const foo = value => Math.pow(value, 2);

let name = `Oleg`;

const y = 8;

module.exports = {x, foo, name, y};

console.log(`at the end variables.js`);